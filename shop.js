function renderShop(){let list=[...products];const q=(document.getElementById('q').value||'').toLowerCase();const cat=document.getElementById('cat').value;const sort=document.getElementById('sort').value;if(q)list=list.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q));if(cat!=='all')list=list.filter(p=>p.type===cat);if(sort==='low')list.sort((a,b)=>a.price-b.price);if(sort==='high')list.sort((a,b)=>b.price-a.price);if(sort==='rating')list.sort((a,b)=>b.rating-a.rating);const grid=document.getElementById('shopGrid');grid.innerHTML=list.length?list.map(productCard).join(''):'<div class="empty"><h2>Không tìm thấy sản phẩm</h2><p>Hãy thử từ khóa hoặc bộ lọc khác.</p></div>';document.getElementById('count').textContent=list.length}
renderShop();
